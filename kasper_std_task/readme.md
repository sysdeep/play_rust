# Kaspersky std task

```python
# система учета финансов, выводит баланс клиента после серии операций
#
# На вход программе подается многострочный текстовый файл.
# В этом докумете одна строчка - набор операций для одного человека (расходы и доходы).
# Строка состоит из имени, разделителя (двоеточие) и серии целых чисел, разделенных пробелом.
# Пример строки:
# alice:5 -3 -20 7
# Расход указан, как отрицательное число. Доход - как положительное число.
# В документе операции над балансом человека могут встречаться несколько раз (более одного).
#
# При этом имя человека может встречаться в разных регистрах (прописные, строчные), это надо игнорировать.
# То есть, записи с именами ALICE, alice, Alice относятся к одному человеку.
#
# Надо посчитать и распечатать баланс по каждому человеку в документе.
#
# В дальнейшем предполагается развитие программы, код должен быть поддерживаемым
#
# Пример документа:
# alice:5 -3 -20 7
# bob:-1 10
# Bob:3


# Возможный результат:
# alice: -11
# bob: 12
```
